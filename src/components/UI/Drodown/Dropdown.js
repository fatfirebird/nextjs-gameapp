import ReactDropdown from 'react-dropdown'
import styled from 'styled-components'
import { space, layout } from 'styled-system'

export const SDropdown = styled(ReactDropdown)`
  ${space}
  ${layout}
`
