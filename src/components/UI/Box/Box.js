import styled from 'styled-components'
import { space, layout, position, flexbox, grid, opacity, padding } from 'styled-system'

export const Box = styled.div`
  ${space}
  ${layout}
  ${flexbox}
  ${grid}
  ${position}
  ${opacity}
  ${padding}
`
